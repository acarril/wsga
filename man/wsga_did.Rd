% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wsga.R
\name{wsga_did}
\alias{wsga_did}
\title{Weighted Subgroup Analysis for Difference-in-Differences Designs}
\usage{
wsga_did(
  formula,
  data,
  unit = NULL,
  time = NULL,
  treat = NULL,
  post_value = NULL,
  balance = NULL,
  ps_model = c("logit", "probit"),
  noipsw = FALSE,
  m = 2L,
  comsup = FALSE,
  show_balance = FALSE,
  bootstrap = TRUE,
  bsreps = 200L,
  boot_type = c("pairs", "wild"),
  inference = NULL,
  block_var = NULL,
  fixed_ps = FALSE,
  seed = NULL,
  vce = "HC1",
  cluster_var = NULL,
  weights = NULL
)
}
\arguments{
\item{formula}{A two-part \code{Formula} of the form
\code{outcome ~ covariates | subgroup}. The left of \code{|} contains covariates
included in the outcome regression; the right of \code{|} is the binary (0/1)
subgroup indicator. If there are no covariates, write \code{outcome ~ 1 | subgroup}.}

\item{data}{A data frame in long format (one row per unit-time).}

\item{unit}{Character: name of the unit identifier column.}

\item{time}{Character: name of the time variable column. Must have exactly
2 unique non-missing values.}

\item{treat}{Character: name of the binary (0/1) treatment-group indicator
(constant within unit).}

\item{post_value}{Optional value of \code{time} that denotes the post period.
Default: \code{max(time)} for numeric/Date columns, or the second sorted value
otherwise.}

\item{balance}{A one-sided formula (e.g. \code{~ m1 + m2}) specifying balance
variables for the propensity score model. Defaults to the covariates in
\code{formula} if not supplied.}

\item{ps_model}{Propensity score model: \code{"logit"} (default) or \code{"probit"}.}

\item{noipsw}{Logical. If \code{TRUE}, skips IPW entirely. Default \code{FALSE}.}

\item{m}{Integer weighting mode when \code{noipsw = FALSE}:
\itemize{
\item \code{2} (default): balance both groups toward the pooled distribution.
\item \code{1}: balance group 1 toward group 0 distribution.
\item \code{0}: balance group 0 toward group 1 distribution.
}}

\item{comsup}{Logical. Restrict to common propensity score support?
Default \code{FALSE}.}

\item{show_balance}{Logical. Print balance tables to the console?
Default \code{FALSE}.}

\item{bootstrap}{Logical. Run the bootstrap loop? Default \code{TRUE}.}

\item{bsreps}{Positive integer: number of bootstrap replications. Default \code{200}.}

\item{boot_type}{Character. Bootstrap resampling scheme: \code{"pairs"} (default)
or \code{"wild"} (unrestricted wild cluster bootstrap with Rademacher signs).
\code{"wild"} is recommended when the number of clusters is small (G < ~30).
See \code{\link[=wsga_rdd]{wsga_rdd()}} for the IPW-uncertainty caveat.}

\item{inference}{Character. One of \code{"empirical"} (default with bootstrap),
\code{"normal"}, or \code{"analytical"} (default without bootstrap).}

\item{block_var}{Character or \code{NULL}. Column name for stratified bootstrap
resampling (strata of clusters when \code{cluster_var} is set). Default \code{NULL}.
Ignored when \code{boot_type = "wild"}.}

\item{fixed_ps}{Logical. Diagnostic flag; see \code{\link[=wsga_rdd]{wsga_rdd()}} for details.
Default \code{FALSE}.}

\item{seed}{Integer or \code{NULL}. RNG seed for reproducibility. Default \code{NULL}.}

\item{vce}{Character: sandwich SE type. Default \code{"cluster"} when
\code{cluster_var} is set, otherwise \code{"HC1"}.}

\item{cluster_var}{Character or \code{NULL}. Column name defining clusters.
Defaults to \code{unit} (pairs-cluster bootstrap over panel units).}

\item{weights}{Character or \code{NULL}. Column name of pre-existing observation
weights.}
}
\value{
An S3 object of class \code{"wsga"}. The structure is identical to
\code{\link[=wsga_rdd]{wsga_rdd()}}; see that function for the full description of return elements.
\code{model_fit} is always an \code{lm} object (sharp DiD only; no IV path).
}
\description{
Estimates per-subgroup treatment effects and their difference in a sharp
2-period difference-in-differences (DiD) design, using inverse probability
weighting (IPW) to balance observed moderators across subgroups. The
estimator is a long-form TWFE regression (unit + time fixed effects) fully
interacted by subgroup.
}
\examples{
data(wsga_did_synth)
# Unweighted DiD subgroup analysis
fit <- wsga_did(y ~ 1 | sgroup, data = wsga_did_synth,
                unit = "unit", time = "time", treat = "D",
                noipsw = TRUE, bootstrap = FALSE)
print(fit)

# IPW-weighted DiD
fit2 <- wsga_did(y ~ m | sgroup, data = wsga_did_synth,
                 unit = "unit", time = "time", treat = "D",
                 bootstrap = FALSE)
summary(fit2)

}
